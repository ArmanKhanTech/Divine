import 'package:flutter/cupertino.dart';

class VideoEditorPlus extends StatefulWidget{
  const VideoEditorPlus({super.key});

  @override
  State<VideoEditorPlus> createState () => _VideoEditorPlusState();
}

class _VideoEditorPlusState extends State<VideoEditorPlus>{
  @override
  Widget build(BuildContext context){

    return Container();
  }
}