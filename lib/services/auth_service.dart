class AuthService {
  Future<bool> loginUser({String? email, String? password}) async {
    return false;
  }

  Future<bool> createUser({String? email, String? password, String? name, String? country}) async {
    return false;
  }
}
